name: Apply habit repository transform

on:
  push:
    branches: [feat/habit-management]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/habit-management
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node scripts/apply-habit-repository.mjs
      - run: node --check src/store.js
      - name: Commit repository changes
        run: |
          if git diff --quiet; then
            echo "No repository changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/store.js
          git commit -m "feat: add habit edit and archive repository support"
          git push origin HEAD:feat/habit-management
