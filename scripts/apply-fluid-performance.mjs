import { readFile, writeFile, readdir } from 'node:fs/promises';

function replaceOnce(source, search, replacement, label = search.slice(0, 60)) {
  const first = source.indexOf(search);
  if (first < 0) throw new Error(`Missing replacement marker: ${label}`);
  if (source.indexOf(search, first + search.length) >= 0) throw new Error(`Ambiguous replacement marker: ${label}`);
  return `${source.slice(0, first)}${replacement}${source.slice(first + search.length)}`;
}

function replaceBetween(source, start, end, replacement, label = start) {
  const first = source.indexOf(start);
  if (first < 0) throw new Error(`Missing start marker: ${label}`);
  const last = source.indexOf(end, first + start.length);
  if (last < 0) throw new Error(`Missing end marker: ${label}`);
  return `${source.slice(0, first)}${replacement}${source.slice(last)}`;
}

let store = await readFile('src/store.js', 'utf8');

store = replaceOnce(store, `  function getState() {\n    return clone(state);\n  }\n`, `  function getState() {\n    return clone(state);\n  }\n\n  function peekState() {\n    return state;\n  }\n\n  function hydrateState(cachedState) {\n    if (!cachedState || cachedState.currentUserId !== user.id) throw new Error('Cached state belongs to a different user');\n    state = clone(cachedState);\n    return state;\n  }\n\n  function syncMemberMetrics(memberId) {\n    if (!memberId) return;\n    const profile = [...(state.personalizedLeague || []), ...(state.members || [])].find((item) => item.id === memberId);\n    const timeZone = profile?.timeZone || 'UTC';\n    const validCheckIns = (state.checkIns || []).filter((checkIn) => checkIn.userId === memberId && !checkIn.invalid);\n    const dates = [...new Set(validCheckIns.map((checkIn) => checkIn.date))];\n    const habitById = new Map((state.habits || []).map((habit) => [habit.id, habit]));\n    const xp = validCheckIns.reduce((total, checkIn) => total + (habitById.get(checkIn.habitId)?.xp || 0), 0);\n    const today = dateInTimezone(timeZone);\n    const update = (item) => {\n      if (item?.id !== memberId) return;\n      item.xp = xp;\n      item.currentStreak = calculateStreak(dates, today);\n      item.bestStreak = calculateBestStreak(dates);\n    };\n    (state.members || []).forEach(update);\n    (state.personalizedLeague || []).forEach(update);\n    (state.friends || []).forEach(update);\n  }\n\n  function syncReactionActivity(checkInId) {\n    const positive = (state.reactions || []).filter((reaction) => reaction.checkInId === checkInId && reaction.emoji !== '👎');\n    const reactionCounts = positive.reduce((counts, reaction) => {\n      counts[reaction.emoji] = (counts[reaction.emoji] || 0) + 1;\n      return counts;\n    }, {});\n    const userReactions = positive.filter((reaction) => reaction.userId === user.id).map((reaction) => reaction.emoji);\n    state.friendActivities = (state.friendActivities || []).map((activity) => activity.checkInId === checkInId\n      ? { ...activity, reactionCounts, userReactions }\n      : activity);\n    state.activities = state.friendActivities;\n  }\n\n  function syncProofVerdict(checkInId) {\n    const checkIn = (state.checkIns || []).find((item) => item.id === checkInId);\n    if (!checkIn) return;\n    const downvoteUsers = new Set((state.reactions || [])\n      .filter((reaction) => reaction.checkInId === checkInId && reaction.emoji === '👎' && reaction.userId !== checkIn.userId)\n      .map((reaction) => reaction.userId));\n    const audienceSize = Math.max(1, checkIn.authorizedViewerIds?.length || state.members?.length || 1);\n    const invalid = rejectedCheckInIds([checkIn], state.reactions || [], state.members?.length || audienceSize, new Map([[checkInId, audienceSize]])).has(checkInId);\n    checkIn.downvotes = downvoteUsers.size;\n    checkIn.userDownvoted = downvoteUsers.has(user.id);\n    checkIn.invalid = invalid;\n    syncMemberMetrics(checkIn.userId);\n    state.friendActivities = (state.friendActivities || []).map((activity) => activity.checkInId === checkInId\n      ? {\n          ...activity,\n          invalid,\n          downvotes: checkIn.downvotes,\n          userDownvoted: checkIn.userDownvoted,\n          message: invalid ? 'Proof got cooked 💀 — run it back.' : (checkIn.note || 'Done. Proof beats promises.'),\n        }\n      : activity);\n    state.activities = state.friendActivities;\n  }\n\n  function normalizeLocalCheckIn(row, habitId, date) {\n    return {\n      id: row.id,\n      habitId: row.habitId || row.habit_id || habitId,\n      userId: row.userId || row.user_id || user.id,\n      date: row.date || row.check_date || date,\n      completedAt: row.completedAt || row.completed_at || new Date().toISOString(),\n      completedQuantity: Number(row.completedQuantity ?? row.completed_quantity ?? 1),\n      proofPath: row.proofPath ?? row.proof_path ?? null,\n      note: row.note ?? null,\n      authorizedViewerIds: row.authorizedViewerIds || row.authorized_viewer_ids || null,\n      invalid: Boolean(row.invalid),\n      downvotes: Number(row.downvotes || 0),\n      userDownvoted: Boolean(row.userDownvoted),\n    };\n  }\n\n  function syncCompletedActivity(checkIn, previousId = null) {\n    let activities = (state.friendActivities || []).filter((activity) => !(activity.type === 'completed' && (activity.checkInId === previousId || activity.checkInId === checkIn?.id)));\n    if (checkIn) {\n      const habit = (state.habits || []).find((item) => item.id === checkIn.habitId);\n      const actor = (state.personalizedLeague || []).find((item) => item.id === checkIn.userId) || (state.members || []).find((item) => item.id === checkIn.userId);\n      activities.push({\n        id: checkIn.id,\n        checkInId: checkIn.id,\n        userId: checkIn.userId,\n        type: 'completed',\n        habitTitle: habit?.title || 'Habit',\n        emoji: habit?.emoji || '⚡',\n        when: checkIn.completedAt,\n        streak: actor?.currentStreak || 0,\n        message: checkIn.invalid ? 'Proof got cooked 💀 — run it back.' : (checkIn.note || 'Done. Proof beats promises.'),\n        proofPath: checkIn.proofPath,\n        invalid: Boolean(checkIn.invalid),\n        downvotes: Number(checkIn.downvotes || 0),\n        userDownvoted: Boolean(checkIn.userDownvoted),\n        reactionCounts: {},\n        userReactions: [],\n      });\n    }\n    state.friendActivities = activities.sort((a, b) => new Date(b.when) - new Date(a.when)).slice(0, 40);\n    state.activities = state.friendActivities;\n  }\n\n  function applyPositiveReaction(checkInId, emoji) {\n    if (emoji != null && !['👏', '🔥', '💪', '😂'].includes(emoji)) throw new Error('Choose a supported reaction');\n    if (!(state.checkIns || []).some((item) => item.id === checkInId)) throw new Error('That update is no longer available');\n    const existing = (state.reactions || []).filter((reaction) => reaction.checkInId === checkInId && reaction.userId === user.id && reaction.emoji !== '👎');\n    const previous = existing[0]?.emoji || null;\n    state.reactions = (state.reactions || []).filter((reaction) => !(reaction.checkInId === checkInId && reaction.userId === user.id && reaction.emoji !== '👎'));\n    if (emoji) state.reactions.push({ id: uid('optimistic-reaction'), checkInId, userId: user.id, emoji, createdAt: new Date().toISOString() });\n    syncReactionActivity(checkInId);\n    return previous;\n  }\n\n  function applyProofDownvote(checkInId, downvoted) {\n    const checkIn = (state.checkIns || []).find((item) => item.id === checkInId);\n    if (!checkIn?.proofPath) throw new Error('Nothing to vote on');\n    if (checkIn.userId === user.id) throw new Error('You cannot cook your own proof 😭');\n    const previous = Boolean(checkIn.userDownvoted);\n    state.reactions = (state.reactions || []).filter((reaction) => !(reaction.checkInId === checkInId && reaction.userId === user.id && reaction.emoji === '👎'));\n    if (downvoted) state.reactions.push({ id: uid('optimistic-downvote'), checkInId, userId: user.id, emoji: '👎', createdAt: new Date().toISOString() });\n    syncProofVerdict(checkInId);\n    return previous;\n  }\n\n  function applySimpleCheckIn(habitId, date, checked, suppliedCheckIn = null) {\n    const habit = ownedHabit(habitId);\n    if (habit.proofMode === 'photo') throw new Error('Photo habits use the proof flow');\n    const index = (state.checkIns || []).findIndex((item) => item.habitId === habitId && item.userId === user.id && item.date === date);\n    const previous = index >= 0 ? clone(state.checkIns[index]) : null;\n    if (!checked) {\n      if (index >= 0) state.checkIns.splice(index, 1);\n      syncMemberMetrics(user.id);\n      syncCompletedActivity(null, previous?.id);\n      return { previous, current: null };\n    }\n    const friendIds = (state.friends || []).map((friend) => friend.id);\n    const viewers = habit.audience === AUDIENCES.ONLY_ME\n      ? [user.id]\n      : habit.audience === AUDIENCES.SELECTED_FRIENDS\n        ? [user.id, ...(habit.selectedFriendIds || [])]\n        : [user.id, ...friendIds];\n    const raw = suppliedCheckIn || previous || {\n      id: uid('optimistic-checkin'),\n      habitId,\n      userId: user.id,\n      date,\n      completedAt: new Date().toISOString(),\n      completedQuantity: fullTargetFor(habit, date),\n      proofPath: null,\n      note: null,\n      authorizedViewerIds: [...new Set(viewers)],\n    };\n    const current = normalizeLocalCheckIn(raw, habitId, date);\n    if (index >= 0) state.checkIns[index] = current;\n    else state.checkIns.push(current);\n    syncMemberMetrics(user.id);\n    syncCompletedActivity(current, previous?.id);\n    return { previous, current: clone(current) };\n  }\n\n  function applyOptimisticComment(checkInId, body, suppliedComment = null) {\n    const cleanBody = validateCommentBody(body);\n    if (!(state.checkIns || []).some((item) => item.id === checkInId)) throw new Error('That check-in is no longer available');\n    const comment = suppliedComment\n      ? { ...suppliedComment, checkInId, authorId: suppliedComment.authorId || user.id, body: cleanBody }\n      : { id: uid('optimistic-comment'), checkInId, circleId: state.circleId || null, authorId: user.id, body: cleanBody, createdAt: new Date().toISOString() };\n    const existingIndex = (state.comments || []).findIndex((item) => item.id === comment.id);\n    if (existingIndex >= 0) state.comments[existingIndex] = comment;\n    else { state.comments ||= []; state.comments.push(comment); }\n    return clone(comment);\n  }\n\n  function replaceOptimisticComment(tempId, serverComment) {\n    const index = (state.comments || []).findIndex((item) => item.id === tempId);\n    if (index < 0) return null;\n    const current = state.comments[index];\n    const normalized = {\n      id: serverComment?.id || tempId,\n      checkInId: serverComment?.checkInId || serverComment?.check_in_id || current.checkInId,\n      circleId: serverComment?.circleId ?? serverComment?.circle_id ?? current.circleId ?? null,\n      authorId: serverComment?.authorId || serverComment?.author_id || user.id,\n      body: serverComment?.body || current.body,\n      createdAt: serverComment?.createdAt || serverComment?.created_at || current.createdAt,\n    };\n    state.comments[index] = normalized;\n    return clone(normalized);\n  }\n\n  function removeOptimisticComment(commentId) {\n    const index = (state.comments || []).findIndex((item) => item.id === commentId);\n    if (index < 0) return null;\n    return clone(state.comments.splice(index, 1)[0]);\n  }\n\n  function applyNudgeRead(nudgeId, readAt = new Date().toISOString()) {\n    const nudge = (state.nudges || []).find((item) => item.id === nudgeId && item.toUserId === user.id);\n    if (!nudge) return null;\n    const previous = nudge.readAt || null;\n    nudge.readAt = readAt;\n    return previous;\n  }\n`, 'supabase getState');

store = replaceBetween(store, `  async function completeWithProof(habitId, date, file) {`, `  async function toggleDownvote(checkInId) {`, `  async function toggleSimpleCheckIn(habitId, date, shouldBeChecked, existingId = null) {\n    const habit = ownedHabit(habitId);\n    if (habit.proofMode === 'photo') throw new Error('Photo habits use the proof flow');\n    if (shouldBeChecked) {\n      const { data, error } = await client.from('check_ins').insert({\n        habit_id: habitId,\n        user_id: user.id,\n        check_date: date,\n        completed_quantity: fullTargetFor(habit, date),\n      }).select('*').single();\n      if (error) throw appError(error, 'Could not complete habit');\n      return data;\n    }\n    let query = client.from('check_ins').delete().eq('user_id', user.id);\n    query = existingId && !String(existingId).startsWith('optimistic-')\n      ? query.eq('id', existingId)\n      : query.eq('habit_id', habitId).eq('check_date', date);\n    const { error } = await query;\n    if (error) throw appError(error, 'Could not undo check-in');\n    return true;\n  }\n\n  async function completeWithProof(habitId, date, file) {`, 'insert simple check-in persistence');

store = replaceBetween(store, `  async function toggleDownvote(checkInId) {`, `  async function recoverHabit(habitId, missedDate, input = {}) {`, `  async function setProofDownvote(checkInId, downvoted) {\n    const checkIn = state.checkIns.find((item) => item.id === checkInId);\n    if (!checkIn?.proofPath) throw new Error('Nothing to vote on');\n    if (checkIn.userId === user.id) throw new Error('You cannot cook your own proof 😭');\n    const { error: deleteError } = await client.from('reactions')\n      .delete()\n      .eq('check_in_id', checkInId)\n      .eq('user_id', user.id)\n      .eq('emoji', '👎');\n    if (deleteError) throw appError(deleteError, 'Could not update vote');\n    if (downvoted) {\n      const { error: insertError } = await client.from('reactions').insert({ check_in_id: checkInId, user_id: user.id, emoji: '👎' });\n      if (insertError) throw appError(insertError, 'Could not downvote proof');\n    }\n    return { checkInId, downvoted: Boolean(downvoted) };\n  }\n\n  async function toggleDownvote(checkInId) {\n    const checkIn = state.checkIns.find((item) => item.id === checkInId);\n    return setProofDownvote(checkInId, !checkIn?.userDownvoted);\n  }\n\n  async function setPositiveReaction(checkInId, emoji) {\n    if (emoji != null && !['👏', '🔥', '💪', '😂'].includes(emoji)) throw new Error('Choose a supported reaction');\n    if (!state.checkIns.some((item) => item.id === checkInId)) throw new Error('That update is no longer available');\n    const { error: deleteError } = await client.from('reactions').delete().eq('check_in_id', checkInId).eq('user_id', user.id).neq('emoji', '👎');\n    if (deleteError) throw appError(deleteError, 'Could not update reaction');\n    if (emoji) {\n      const { data, error: insertError } = await client.from('reactions').insert({ check_in_id: checkInId, user_id: user.id, emoji }).select('*').single();\n      if (insertError) throw appError(insertError, 'Could not react');\n      return data;\n    }\n    return { check_in_id: checkInId, user_id: user.id, emoji: null };\n  }\n\n  async function toggleReaction(checkInId, emoji) {\n    if (!['👏', '🔥', '💪', '😂'].includes(emoji)) throw new Error('Choose a supported reaction');\n    const existing = state.reactions.find((reaction) => reaction.checkInId === checkInId && reaction.userId === user.id && reaction.emoji !== '👎');\n    return setPositiveReaction(checkInId, existing?.emoji === emoji ? null : emoji);\n  }\n\n`, 'reaction and rejection persistence');

store = replaceBetween(store, `  async function markNudgeRead(nudgeId) {`, `  async function startBaton(recipientUserId, sourceCheckInId) {`, `  async function markNudgeRead(nudgeId) {\n    const readAt = new Date().toISOString();\n    const { error } = await client.from('nudges').update({ read_at: readAt }).eq('id', nudgeId).eq('to_user_id', user.id);\n    if (error) throw appError(error, 'Could not mark nudge read');\n    return { id: nudgeId, readAt };\n  }\n\n`, 'nudge read persistence');

store = replaceBetween(store, `  async function addComment(checkInId, body) {`, `  function getEarnedBadges(options = {}) {`, `  async function addComment(checkInId, body) {\n    if (!state.checkIns.some((item) => item.id === checkInId)) throw new Error('That check-in is no longer available');\n    const cleanBody = validateCommentBody(body);\n    const { data, error } = await client.rpc('add_check_in_comment', { target_check_in_id: checkInId, comment_body: cleanBody });\n    if (error) throw appError(error, 'Could not add comment');\n    return {\n      id: data?.id || data,\n      checkInId,\n      circleId: data?.circle_id ?? state.circleId ?? null,\n      authorId: data?.author_id || user.id,\n      body: data?.body || cleanBody,\n      createdAt: data?.created_at || new Date().toISOString(),\n    };\n  }\n\n  async function deleteComment(commentId) {\n    const comment = state.comments.find((item) => item.id === commentId);\n    if (!comment || comment.authorId !== user.id) throw new Error('You can only delete your own comment');\n    const { error } = await client.rpc('delete_check_in_comment', { target_comment_id: commentId });\n    if (error) throw appError(error, 'Could not delete comment');\n    return true;\n  }\n\n`, 'comment persistence');

store = replaceOnce(store, `    const friendState = await loadFriends();\n    const normalizedAudience = normalizeAudience(audience, selectedFriendIds, user.id, friendState.friendships);`, `    const normalizedAudience = normalizeAudience(audience, selectedFriendIds, user.id, state.friendships || []);`, 'setHabitAudience friend reload');

store = replaceOnce(store, `    getState,\n    load,`, `    getState,\n    peekState,\n    hydrateState,\n    applyPositiveReaction,\n    applyProofDownvote,\n    applySimpleCheckIn,\n    applyOptimisticComment,\n    replaceOptimisticComment,\n    removeOptimisticComment,\n    applyNudgeRead,\n    load,`, 'repository state methods');
store = replaceOnce(store, `    toggleHabit,\n    completeWithProof,\n    toggleDownvote,\n    toggleReaction,`, `    toggleHabit,\n    toggleSimpleCheckIn,\n    completeWithProof,\n    setProofDownvote,\n    toggleDownvote,\n    setPositiveReaction,\n    toggleReaction,`, 'repository hot persistence methods');

await writeFile('src/store.js', store);

let app = await readFile('src/app.js', 'utf8');
app = replaceOnce(app, `import { createRefreshCoordinator } from './refresh.js';\n`, `import { createRefreshCoordinator } from './refresh.js';\nimport { createLatestIntentCoordinator } from './optimistic.js';\nimport { clearStateCache, readStateCache, writeStateCache } from './state-cache.js';\n`, 'performance imports');
app = replaceOnce(app, `const getState = () => repo?.getState();`, `const getState = () => repo?.peekState?.() || repo?.getState();`, 'fast state accessor');
app = replaceOnce(app, `let retryMutation = null;\nconst screenScroll`, `let retryMutation = null;\nlet networkBootLoading = false;\nlet reconciliationTimer = null;\nlet commentRetryDraft = null;\nlet inviteShareBusy = false;\nconst optimisticPatches = new Map();\nconst screenScroll`, 'performance state variables');

app = replaceOnce(app, `function readableError(error) {\n  return error?.message || 'Something went wrong';\n}\n`, `function readableError(error) {\n  return error?.message || 'Something went wrong';\n}\n\nconst yieldToPaint = () => new Promise((resolve) => {\n  if (typeof requestAnimationFrame === 'function') requestAnimationFrame(() => resolve());\n  else queueMicrotask(resolve);\n});\n\nfunction reapplyOptimisticPatches() {\n  for (const apply of optimisticPatches.values()) {\n    try { apply(); } catch { /* Authoritative refresh may make a stale patch irrelevant. */ }\n  }\n}\n\nfunction scheduleReconciliation(delay = 650) {\n  clearTimeout(reconciliationTimer);\n  reconciliationTimer = setTimeout(() => { void refreshCoordinator?.request('optimistic'); }, delay);\n}\n\nfunction scheduleStateCacheWrite(activeRepo = repo) {\n  const userId = session?.user?.id;\n  if (!userId || !activeRepo) return;\n  const write = () => {\n    if (repo !== activeRepo || session?.user?.id !== userId) return;\n    void writeStateCache(userId, activeRepo.getState());\n  };\n  if (typeof requestIdleCallback === 'function') requestIdleCallback(write, { timeout: 1500 });\n  else setTimeout(write, 0);\n}\n\nasync function runOptimisticMutation({ key, apply, rollback, persist, errorMessage = 'Could not save that change', onSuccess = null }) {\n  if (!online) {\n    notify('You are offline. Nothing was saved yet.', 3200);\n    return undefined;\n  }\n  if (optimisticPatches.has(key)) return undefined;\n  optimisticPatches.set(key, apply);\n  apply();\n  renderPreservingScroll();\n  await yieldToPaint();\n  try {\n    const result = await persist();\n    optimisticPatches.delete(key);\n    if (typeof onSuccess === 'function') onSuccess(result);\n    scheduleReconciliation();\n    return result;\n  } catch (error) {\n    optimisticPatches.delete(key);\n    rollback();\n    renderPreservingScroll();\n    notify(readableError(error) || errorMessage, 3600);\n    return undefined;\n  }\n}\n`, 'optimistic helpers');

app = replaceOnce(app, `async function refreshRepositoryData(activeRepo) {\n  await activeRepo.load();\n  if (!session || repo !== activeRepo) return;\n  lastRefreshAt = new Date().toISOString();\n  if (!hasUnsavedDraft()) renderPreservingScroll();\n}`, `async function refreshRepositoryData(activeRepo) {\n  await activeRepo.load();\n  if (!session || repo !== activeRepo) return;\n  reapplyOptimisticPatches();\n  lastRefreshAt = new Date().toISOString();\n  scheduleStateCacheWrite(activeRepo);\n  if (!hasUnsavedDraft()) renderPreservingScroll();\n}`, 'refresh reconciliation');

app = replaceOnce(app, `  if (busy) return undefined;\n  if (!online) {`, `  if (busy || networkBootLoading) {\n    if (networkBootLoading) notify('Finishing the latest sync…', 1800);\n    return undefined;\n  }\n  if (!online) {`, 'authoritative boot gate');

app = replaceOnce(app, `<input name=\"body\" maxlength=\"180\" required autocomplete=\"off\" placeholder=\"Say something useful…\">`, `<input name=\"body\" maxlength=\"180\" required autocomplete=\"off\" placeholder=\"Say something useful…\" value=\"\${commentRetryDraft?.checkInId === commentCheckInId ? esc(commentRetryDraft.body) : ''}\">`, 'comment retry draft');

app = replaceBetween(app, `async function handleUndoCheckIn(habitId, date = today()) {`, `function closeHabitEditor() {`, `async function handleSimpleCheckIn(habit, date, checked, successMessage = '') {\n  const current = checkInFor(habit.id, me()?.id, date);\n  const existingId = current?.id || null;\n  const previous = current ? { ...current } : null;\n  const key = \`checkin:\${habit.id}:\${date}\`;\n  const result = await runOptimisticMutation({\n    key,\n    apply: () => repo.applySimpleCheckIn(habit.id, date, checked),\n    rollback: () => repo.applySimpleCheckIn(habit.id, date, Boolean(previous), previous),\n    persist: () => repo.toggleSimpleCheckIn(habit.id, date, checked, existingId),\n    errorMessage: checked ? 'Could not complete habit' : 'Could not undo check-in',\n    onSuccess: (serverRow) => {\n      if (checked && serverRow && typeof serverRow === 'object') repo.applySimpleCheckIn(habit.id, date, true, serverRow);\n    },\n  });\n  if (result === undefined) return undefined;\n  haptic(checked ? 28 : 16);\n  if (successMessage) notify(successMessage, 4200, checked ? { action: { label: 'Undo', onClick: () => handleUndoCheckIn(habit.id, date) } } : {});\n  renderPreservingScroll();\n  return result;\n}\n\nasync function handleUndoCheckIn(habitId, date = today()) {\n  const habit = getState()?.habits.find((item) => item.id === habitId);\n  const checkIn = checkInFor(habitId, me()?.id, date);\n  if (!habit || !checkIn) return;\n  if (habit.proofMode === 'photo' || checkIn.proofPath) {\n    await runMutation(() => repo.toggleHabit(habitId, date), 'Check-in undone');\n    return;\n  }\n  await handleSimpleCheckIn(habit, date, false, 'Check-in undone');\n}\n\nasync function handleHabit(id) {\n  const habit = getState().habits.find((item) => item.id === id);\n  if (!habit) return;\n  const checkInDate = today();\n  const current = checkInFor(id, me()?.id, checkInDate);\n  if (current && !current.invalid) {\n    if (habit.proofMode === 'photo' || current.proofPath) {\n      await runMutation(() => repo.toggleHabit(id, checkInDate), \`\${habit.title} unchecked\`);\n      return;\n    }\n    await handleSimpleCheckIn(habit, checkInDate, false, \`\${habit.title} unchecked\`);\n    return;\n  }\n  if (habit.proofMode === 'photo') {\n    proofHabit = id;\n    render();\n    return;\n  }\n  await handleSimpleCheckIn(habit, checkInDate, true, \`Checked in · \${habit.title}\`);\n}\n\n`, 'simple optimistic check-in handlers');

app = replaceBetween(app, `async function handleDownvote(checkInId) {`, `async function handleBatonSubmit(event) {`, `async function handleDownvote(checkInId) {\n  const checkIn = getState()?.checkIns.find((item) => item.id === checkInId);\n  if (!checkIn) return;\n  const previous = Boolean(checkIn.userDownvoted);\n  const desired = !previous;\n  const result = await runOptimisticMutation({\n    key: \`downvote:\${checkInId}\`,\n    apply: () => repo.applyProofDownvote(checkInId, desired),\n    rollback: () => repo.applyProofDownvote(checkInId, previous),\n    persist: () => repo.setProofDownvote(checkInId, desired),\n    errorMessage: 'Could not update proof vote',\n  });\n  if (result !== undefined) haptic(18);\n}\n\nfunction patchReactionDom(checkInId) {\n  const activity = activityList(getState()).find((item) => item.checkInId === checkInId);\n  if (!activity) return;\n  const buttons = [...app.querySelectorAll('[data-reaction]')].filter((button) => button.dataset.reaction === checkInId);\n  const mine = (activity.userReactions || []).slice(-1);\n  const counts = activity.reactionCounts || {};\n  buttons.forEach((button) => {\n    const emoji = button.dataset.reactionEmoji;\n    const active = mine.includes(emoji);\n    button.classList.toggle('active', active);\n    button.setAttribute('aria-pressed', String(active));\n    const count = button.querySelector('span');\n    if (count) count.textContent = String(counts[emoji] || 0);\n  });\n  const total = Object.values(counts).reduce((sum, count) => sum + Number(count || 0), 0);\n  const summary = mine.length\n    ? \`You reacted \${mine[0]} · \${total} \${total === 1 ? 'reaction' : 'reactions'}\`\n    : total ? \`\${total} \${total === 1 ? 'reaction' : 'reactions'}\` : 'Be the first to hype this';\n  const summaryNode = buttons[0]?.closest('.activity-social-actions')?.querySelector('.reaction-summary');\n  if (summaryNode) summaryNode.textContent = summary;\n}\n\nconst reactionCoordinator = createLatestIntentCoordinator({\n  persist: async (key, desired) => repo.setPositiveReaction(key.slice('reaction:'.length), desired),\n  onConfirmed: () => scheduleReconciliation(),\n  onError: ({ key, confirmed, error }) => {\n    const checkInId = key.slice('reaction:'.length);\n    optimisticPatches.delete(key);\n    try { repo.applyPositiveReaction(checkInId, confirmed); } catch { /* The proof may have disappeared. */ }\n    patchReactionDom(checkInId);\n    notify(readableError(error), 3600);\n  },\n});\n\nasync function handleReaction(checkInId, emoji) {\n  if (!online) {\n    notify('You are offline. Nothing was saved yet.', 3200);\n    return;\n  }\n  const activity = activityList(getState()).find((item) => item.checkInId === checkInId);\n  if (!activity) return;\n  const current = (activity.userReactions || []).slice(-1)[0] || null;\n  const desired = current === emoji ? null : emoji;\n  const key = \`reaction:\${checkInId}\`;\n  repo.applyPositiveReaction(checkInId, desired);\n  optimisticPatches.set(key, () => repo.applyPositiveReaction(checkInId, reactionCoordinator.desired(key)));\n  patchReactionDom(checkInId);\n  haptic(18);\n  reactionCoordinator.queue(key, desired, { confirmed: reactionCoordinator.confirmed(key) ?? current });\n  void reactionCoordinator.whenIdle(key).then(() => {\n    if (!reactionCoordinator.isPending(key)) {\n      optimisticPatches.delete(key);\n      scheduleReconciliation();\n    }\n  });\n}\n\nasync function handleCommentSubmit(event) {\n  event.preventDefault();\n  const checkInId = commentCheckInId;\n  const form = new FormData(event.currentTarget);\n  const body = String(form.get('body') || '').trim();\n  if (!checkInId || !body || !online) {\n    if (!online) notify('You are offline. Nothing was saved yet.', 3200);\n    return;\n  }\n  const key = \`comment:add:\${checkInId}\`;\n  if (optimisticPatches.has(key)) return;\n  const temp = repo.applyOptimisticComment(checkInId, body);\n  optimisticPatches.set(key, () => repo.applyOptimisticComment(checkInId, body, temp));\n  commentRetryDraft = null;\n  renderPreservingScroll();\n  await yieldToPaint();\n  try {\n    const saved = await repo.addComment(checkInId, body);\n    repo.replaceOptimisticComment(temp.id, saved);\n    optimisticPatches.delete(key);\n    scheduleReconciliation();\n    notify('Reply sent');\n  } catch (error) {\n    optimisticPatches.delete(key);\n    repo.removeOptimisticComment(temp.id);\n    commentRetryDraft = { checkInId, body };\n    renderPreservingScroll();\n    notify(readableError(error), 3600);\n  }\n}\n\nasync function handleUndoCommentDelete(comment) {\n  if (!comment) return;\n  commentRetryDraft = { checkInId: comment.checkInId, body: comment.body };\n  const fakeEvent = { preventDefault() {}, currentTarget: { entries: undefined } };\n  const restored = repo.applyOptimisticComment(comment.checkInId, comment.body, comment);\n  try {\n    const saved = await repo.addComment(comment.checkInId, comment.body);\n    repo.replaceOptimisticComment(restored.id, saved);\n    commentRetryDraft = null;\n    scheduleReconciliation();\n    renderPreservingScroll();\n    notify('Reply restored');\n  } catch (error) {\n    repo.removeOptimisticComment(restored.id);\n    renderPreservingScroll();\n    notify(readableError(error), 3600);\n  }\n  void fakeEvent;\n}\n\nasync function handleDeleteComment(commentId) {\n  const comment = (getState().comments || []).find((item) => item.id === commentId);\n  if (!comment || !online) return;\n  const key = \`comment:delete:\${commentId}\`;\n  if (optimisticPatches.has(key)) return;\n  const removed = repo.removeOptimisticComment(commentId);\n  optimisticPatches.set(key, () => repo.removeOptimisticComment(commentId));\n  renderPreservingScroll();\n  await yieldToPaint();\n  try {\n    await repo.deleteComment(commentId);\n    optimisticPatches.delete(key);\n    scheduleReconciliation();\n    notify('Reply deleted', 5000, { action: { label: 'Undo', onClick: () => handleUndoCommentDelete(comment) } });\n  } catch (error) {\n    optimisticPatches.delete(key);\n    if (removed) repo.applyOptimisticComment(removed.checkInId, removed.body, removed);\n    renderPreservingScroll();\n    notify(readableError(error), 3600);\n  }\n}\n\n`, 'optimistic social handlers');

app = replaceBetween(app, `async function handleReadNudge(nudgeId) {`, `async function handleDisplayName(event) {`, `async function markNudgeReadOptimistic(nudgeId) {\n  const nudge = (getState()?.nudges || []).find((item) => item.id === nudgeId);\n  if (!nudge || nudge.readAt) return true;\n  const readAt = new Date().toISOString();\n  const previous = nudge.readAt || null;\n  return runOptimisticMutation({\n    key: \`nudge-read:\${nudgeId}\`,\n    apply: () => repo.applyNudgeRead(nudgeId, readAt),\n    rollback: () => repo.applyNudgeRead(nudgeId, previous),\n    persist: () => repo.markNudgeRead(nudgeId),\n    errorMessage: 'Could not mark nudge read',\n  });\n}\n\nasync function handleReadNudge(nudgeId) {\n  await markNudgeReadOptimistic(nudgeId);\n}\n\n`, 'optimistic nudge read');

app = replaceBetween(app, `async function loadProofViewerUrl() {`, `async function loadProofThumbnail(element) {`, `async function loadProofViewerUrl() {\n  const current = proofViewer;\n  if (!current) return;\n  const requestId = ++proofViewerRequestId;\n  const cachedUrl = proofThumbnailUrls.get(current.path) || null;\n  if (cachedUrl) {\n    proofViewer = { ...current, status: 'ready', url: cachedUrl, error: null };\n    render();\n    return;\n  }\n  proofViewer = { ...current, status: 'loading', url: null, error: null };\n  render();\n  try {\n    const url = await repo.getProofUrl(current.path);\n    if (requestId !== proofViewerRequestId || !proofViewer || proofViewer.path !== current.path) return;\n    proofThumbnailUrls.set(current.path, url);\n    proofViewer = { ...proofViewer, status: 'ready', url, error: null };\n  } catch (error) {\n    if (requestId !== proofViewerRequestId || !proofViewer || proofViewer.path !== current.path) return;\n    proofViewer = { ...proofViewer, status: 'error', url: null, error: readableError(error) };\n  }\n  render();\n}\n\n`, 'proof viewer URL cache');

app = replaceOnce(app, `  app.querySelectorAll('[data-invite-from-people]').forEach((element) => { element.onclick = () => { peopleSheetOpen = true; inviteSheetOpen = true; render(); }; });`, `  app.querySelectorAll('[data-invite-from-people]').forEach((element) => { element.onclick = handleShareInvite; });`, 'direct friends share binding');

app = replaceBetween(app, `async function handleCreateFriendInvite() {`, `function clearPendingInvite() {`, `async function handleCreateFriendInvite() {\n  if (typeof repo?.createFriendInvite !== 'function' || inviteShareBusy) return null;\n  inviteShareBusy = true;\n  try {\n    return await repo.createFriendInvite();\n  } catch (error) {\n    notify(readableError(error), 3600);\n    return null;\n  } finally {\n    inviteShareBusy = false;\n  }\n}\n\nasync function handleShareInvite() {\n  const directFriendFlow = typeof repo?.createFriendInvite === 'function';\n  let code = '';\n  let url = '';\n  if (directFriendFlow) {\n    const invite = await handleCreateFriendInvite();\n    if (!invite) return;\n    code = typeof invite === 'string' ? invite : invite.code || invite.inviteCode || '';\n    if (!validateInviteCode(code).valid) {\n      notify('Invite code is not ready yet. Try again in a sec.', 3200);\n      return;\n    }\n    url = (typeof invite === 'object' && invite?.url) || buildInviteLink(window.location.href, code);\n  } else {\n    code = activeInviteCode();\n    if (!validateInviteCode(code).valid) {\n      notify('Invite code is not ready yet. Try again in a sec.', 3200);\n      return;\n    }\n    url = activeInviteUrl() || buildInviteLink(window.location.href, code);\n  }\n  const payload = {\n    title: 'Join me on Donezo',\n    text: 'Join me on Donezo. We’re trying to actually lock in.',\n    url,\n  };\n  if (typeof navigator.share === 'function') {\n    try {\n      await navigator.share(payload);\n      return;\n    } catch (error) {\n      if (error?.name === 'AbortError') return;\n    }\n  }\n  try {\n    await navigator.clipboard.writeText(url);\n    notify('Invite link copied');\n  } catch {\n    notify(url, 6000);\n  }\n}\n\n`, 'direct share flow');

app = replaceBetween(app, `async function handleSignOut() {`, `proofInput.addEventListener('change'`, `async function handleSignOut() {\n  const userId = session?.user?.id;\n  bootGeneration += 1;\n  stopRefreshCoordinator();\n  clearProofReview();\n  proofHabit = null;\n  proofViewer = null;\n  optimisticPatches.clear();\n  clearTimeout(reconciliationTimer);\n  if (userId) await clearStateCache(userId);\n  await supabase.auth.signOut();\n}\n\n`, 'cache-aware signout');

app = replaceBetween(app, `async function boot(nextSession) {`, `for (const eventName of ['gesturestart', 'gesturechange', 'gestureend']) {`, `async function boot(nextSession) {\n  const generation = ++bootGeneration;\n  stopRefreshCoordinator();\n  clearProofReview();\n  proofHabit = null;\n  proofViewer = null;\n  optimisticPatches.clear();\n  clearTimeout(reconciliationTimer);\n  session = nextSession;\n  online = navigator.onLine !== false;\n  networkBootLoading = false;\n  if (!session) {\n    repo = null;\n    lastRefreshAt = null;\n    render();\n    return;\n  }\n  app.innerHTML = '<div class=\"standalone-screen loading\"><div class=\"brand\"><span>ϟ</span><strong>Donezo</strong></div><p>Loading your circle…</p></div>';\n  const activeRepo = createSupabaseRepository(supabase, session.user);\n  repo = activeRepo;\n  let cachedRendered = false;\n  const cachedState = await readStateCache(session.user.id);\n  if (generation !== bootGeneration || nextSession?.user?.id !== session?.user?.id) return;\n  if (cachedState) {\n    try {\n      activeRepo.hydrateState(cachedState);\n      cachedRendered = true;\n      render();\n    } catch {\n      cachedRendered = false;\n    }\n  }\n  networkBootLoading = true;\n  try {\n    await activeRepo.load((!initialNavigationHandled && initialNavigation.circleId) || localStorage.getItem('donezo.activeSquadId') || undefined);\n    if (generation !== bootGeneration || nextSession?.user?.id !== session?.user?.id) return;\n    networkBootLoading = false;\n    reapplyOptimisticPatches();\n    lastRefreshAt = new Date().toISOString();\n    render();\n    scheduleStateCacheWrite(activeRepo);\n    startRefreshCoordinator(activeRepo);\n    await applyInitialNavigation();\n    if (getNotificationCapability(window).permission === 'granted') syncPushSubscription(activeRepo).catch(() => {});\n  } catch (error) {\n    if (generation !== bootGeneration || nextSession?.user?.id !== session?.user?.id) return;\n    networkBootLoading = false;\n    if (cachedRendered) {\n      startRefreshCoordinator(activeRepo);\n      notify(`Could not refresh. Showing your last good sync. \${readableError(error)}`, 4200);\n      renderPreservingScroll();\n      return;\n    }\n    stopRefreshCoordinator();\n    app.innerHTML = \`<div class=\"standalone-screen loading\"><div class=\"brand\"><span>ϟ</span><strong>Donezo</strong></div><h1>Could not load.</h1><p>\${esc(readableError(error))}</p><button class=\"btn primary\" id=\"retry\">Retry</button><button class=\"text-btn\" id=\"sign-out\">Sign out</button></div>\`;\n    app.querySelector('#retry')?.addEventListener('click', () => boot(session));\n    app.querySelector('#sign-out')?.addEventListener('click', handleSignOut);\n  }\n}\n\n`, 'warm boot');

await writeFile('src/app.js', app);

let social = await readFile('social.css', 'utf8');
social = replaceOnce(social, `.people-growth-actions{display:grid;gap:var(--space-2);margin-top:var(--space-4)}`, `.people-growth-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:var(--space-2);margin-top:var(--space-4)}.people-growth-actions>.btn{min-width:0}@media(max-width:320px){.people-growth-actions{grid-template-columns:1fr}}`, 'friends action grid');
await writeFile('social.css', social);

let sw = await readFile('sw.js', 'utf8');
sw = replaceOnce(sw, `const CACHE = 'donezo-shell-v24';`, `const CACHE = 'donezo-shell-v25';`, 'PWA v25');
await writeFile('sw.js', sw);

const testFiles = await readdir('test');
for (const name of testFiles.filter((name) => name.endsWith('.test.mjs'))) {
  const path = `test/${name}`;
  const source = await readFile(path, 'utf8');
  if (source.includes('donezo-shell-v24')) await writeFile(path, source.replaceAll('donezo-shell-v24', 'donezo-shell-v25'));
}
